#include <stdio.h>

int main () {
// a)
    //3ab - Variável não pode começar com números
// b)
    int sim = 1;
// c)
    int n_a_o = 2;
// d)
    // 98Fim - Variável não pode começar com números
// e)
    // int - Não pode usar palavras reservadas nos nomes de variáveis
// f)
    int A123 = 3;
// g)
    // x**x - Não pode usar caractéres especiais nos nomes de variáveis
// h)
    int __A = 4;
// i)
    // --A - Não pode usar caractéres especiais nos nomes de variáveis
// j)
    int y_2 = 5;
// k)
    int YYFim = 6;
// l)
    // \meu - Não pode usar caractéres especiais nos nomes de variáveis
// m)
    // *y2 - Não pode usar caractéres especiais nos nomes de variáveis
// n)
    // float - Não pode usar palavras reservadas nos nomes de variáveis
// o)
    int velocidade_media = 7;
    return 0;
}